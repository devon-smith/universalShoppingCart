# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
out/
.next/
.turbo/
.output/
.wxt/
*.tsbuildinfo

# Extension release artifacts
*.crx
*.pem
*.zip

# Environment / secrets
.env
.env.*
!.env.example
*.key
*.pem

# Supabase local state
supabase/.temp/
supabase/.branches/

# Test output
coverage/
playwright-report/
test-results/
blob-report/
.playwright/
playwright/.cache/

# Retailer pages saved into the repository root by a browser. Chrome wrote ten unsanitized
# product pages here once, and only a working-tree check caught them before they were
# committed. Root level only — nothing under apps/ or packages/ is affected.
/*.html

# Live-testing scratch space. Raw saved pages, screenshots, and notes taken while
# testing against real retailers — working files, not fixtures. A fixture is sanitized,
# minimized, and committed under packages/extractors/src/fixtures/; anything here has not
# been through that and may still carry account details, order history, or tokens.
.live/

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
