/** @type {import('prettier').Config} */
export const prettierConfig = {
  printWidth: 100,
  singleQuote: true,
  semi: true,
  trailingComma: 'all',
  arrowParens: 'always',
};

export default prettierConfig;
